{
    "name": "taptapstats",
    "description": "Personal writing statistics.",
    "version": "0.0.0",
    "homepage": "https://github.com/angular/taptapstats",
    "license": "MIT",
    "private": true,
    "scripts": {
        "postinstall": "bower install && virtualenv -p python3 --clear venv",
        "prestart": "venv/bin/pip install -U .",
        "start": "venv/bin/twist taptap --endpoint=tcp:8080"
    }
}
