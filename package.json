{
    "name": "taptapstats",
    "description": "Personal writing statistics.",
    "version": "0.0.0",
    "homepage": "https://github.com/angular/taptapstats",
    "license": "MIT",
    "private": true,
    "scripts": {
        "postinstall": "bower install && virtualenv -p python3 --clear venv && venv/bin/pip install -r requirements.txt",
        "prestart": "venv/bin/pip install .",
        "start": "venv/bin/twist -m"
    }
}
